% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pib_pollution_log.R
\name{categorize}
\alias{categorize}
\title{Assign a loose "potential category" label to each release}
\usage{
categorize(title, body, cfg)
}
\arguments{
\item{title}{Character vector of titles.}

\item{body}{Character vector of bodies, same length as \code{title}.}

\item{cfg}{Config list from \code{\link[=load_config]{load_config()}}, with a \code{categories} block.}
}
\value{
Character vector of category names, same length as \code{title}.
}
\description{
For each release, walks \code{cfg$categories} in order and returns the name
of the FIRST category whose \code{match} words appear in the title or body
(same matching rules as elsewhere: case-insensitive, word-bounded,
hyphen-tolerant, auto-plural). Anything matching no category gets the
fallback category (the one with an empty \code{match} list, e.g.
"Other env/pollution").
}
\details{
This is a rough guide for browsing, not a hard classification — hence
the sheet column is named "potential category".
}
\examples{
\dontrun{
categorize(releases$title, releases$body, cfg)
}
}
