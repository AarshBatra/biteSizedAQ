% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pib_pollution_log.R
\name{escape_regex}
\alias{escape_regex}
\title{Escape regex metacharacters in a string}
\usage{
escape_regex(x)
}
\arguments{
\item{x}{Character vector to escape.}
}
\value{
\code{x} with regex metacharacters backslash-escaped.
}
\description{
Keywords from the config are user-supplied plain text (e.g. "PM2.5"),
so any regex metacharacters must be escaped before the keywords are
combined into a matching pattern.
}
\details{
\code{perl = TRUE} is required. In POSIX bracket expressions (R's default
engine) a backslash is a literal character rather than an escape, so
the class below terminates early and the trailing \code{{}} is misread as
an interval quantifier.
}
\examples{
escape_regex("PM2.5")
}
