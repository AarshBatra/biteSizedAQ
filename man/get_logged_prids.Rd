% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pib_pollution_log.R
\name{get_logged_prids}
\alias{get_logged_prids}
\title{Get PRIDs already present in the sheet}
\usage{
get_logged_prids(sheet_id, tab)
}
\arguments{
\item{sheet_id}{Google Sheet ID.}

\item{tab}{Worksheet name.}
}
\value{
Character vector of logged PRIDs; empty if none/tab missing.
}
\description{
Returns an empty vector if the tab doesn't exist yet (normal on a
first run). If the tab exists but has no \code{prid} column, that's a
malformed sheet — usually a header row that never got written — and
it warns rather than failing silently, because the consequence is
that deduplication stops working and every run appends duplicates.
}
\examples{
\dontrun{
get_logged_prids("sheet-id", "Log")
}
}
