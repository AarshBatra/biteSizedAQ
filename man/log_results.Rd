% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pib_pollution_log.R
\name{log_results}
\alias{log_results}
\title{Append matches to the sheet, or a "No PIB" placeholder}
\usage{
log_results(sheet_id, tab, run_date, matches)
}
\arguments{
\item{sheet_id}{Google Sheet ID.}

\item{tab}{Worksheet name.}

\item{run_date}{Date of this run (Date or character).}

\item{matches}{Tibble with \code{date}, \code{ministry}, \code{title}, \code{link}, \code{prid}.}
}
\value{
Invisibly NULL; called for its side effect.
}
\description{
Dedupes real releases against PRIDs already logged. The placeholder
logic is day-aware, which matters when the job runs several times a
day (e.g. every 4 hours):
\itemize{
\item A \code{"No PIB"} row is written at most ONCE per day — later empty runs
the same day do nothing, so a quiet day leaves one placeholder, not
one per run.
\item If a later run the same day finds a real match, any \code{"No PIB"} row
already written for today is removed first, so the day isn't
simultaneously marked empty and non-empty.
}
}
\examples{
\dontrun{
log_results("sheet-id", "Log", Sys.Date(), matches)
}
}
