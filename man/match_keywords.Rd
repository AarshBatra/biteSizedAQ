% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pib_pollution_log.R
\name{match_keywords}
\alias{match_keywords}
\title{Test whether text matches any configured keyword}
\usage{
match_keywords(text, cfg, title_text = NULL)
}
\arguments{
\item{text}{Character vector to test (title or body).}

\item{cfg}{Config list from \code{\link[=load_config]{load_config()}}.}

\item{title_text}{Optional titles, same length as \code{text}, for the
exclusion layer and its strong-keyword guard.}
}
\value{
Logical vector, same length as \code{text}.
}
\description{
Combines several match layers and one guarded exclusion layer:
\itemize{
\item \code{cfg$keywords} — strong terms, matched standalone.
\item \code{cfg$contextual_keywords} + \code{cfg$anchors} — vague terms (e.g.
"water", "green") matched only when near an anchor. Kept for
backward compatibility.
\item \code{cfg$contextual_pairs} — a general list of \{terms, anchors\} groups.
Each group's terms match only when near that group's anchors. This
is how, e.g., health words ("asthma", "respiratory") are required to
co-occur with a pollution word, so "childhood asthma from vehicular
emissions" matches but "new asthma drug approved" does not.
\item \code{cfg$exclude_title_patterns} — structural non-topical titles dropped
UNLESS the title itself carries a strong keyword (guard).
}
}
\details{
Missing config fields are skipped, so older configs keep working.
}
\examples{
\dontrun{
match_keywords(releases$body, cfg, title_text = releases$title)
}
}
